#include <bits/stdc++.h>
using namespace std;
using ll = long long;
const int N = 1005;

int main(int argc, char const *argv[])
{
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);

    return 0;
}
